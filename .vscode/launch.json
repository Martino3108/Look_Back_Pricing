{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run main (C++20 + OpenMP)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/main",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build project (C++20 + OpenMP)",
      "MIMode": "lldb",
      "stopAtEntry": false,
      "externalConsole": false
    }
  ]
}
